name: image_picker
description: A new flutter plugin project.
version: 0.0.1
author:
homepage:

flutter:
  plugin:
    androidPackage: com.najeira.imagepicker
    pluginClass: ImagePickerPlugin

dependencies:
  flutter:
    sdk: flutter
