name: device_info
description: A new flutter plugin project.
version: 0.0.1
author:
homepage:

flutter:
  plugin:
    androidPackage: com.najeira.deviceinfo
    pluginClass: DeviceInfoPlugin

dependencies:
  flutter:
    sdk: flutter
